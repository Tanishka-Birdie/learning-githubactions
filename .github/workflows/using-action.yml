name: using-action
on: push

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v1
    - uses: Tanishka-Birdie/creating-action@main
      env:
       GITHUB_TOKEN: ${{secrets.S_TOKEN}}
      with:
        args: 'FIXED'
